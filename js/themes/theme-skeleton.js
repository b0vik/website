class BackgroundTheme {
    constructor(ctx, canvas, gl) {
        this.ctx = ctx;
        this.canvas = canvas;
        this.gl = gl;
    }
 
    start() {
        // Implement this method in subclasses
    }
 }